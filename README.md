# Tic-Tac-Foe — Legal

Public host for the Tic-Tac-Foe Android app's legal pages (the app's
source lives in a separate private repo). Served via GitHub Pages.

- `privacy-policy.html`
- `account-deletion.html`

Edit the source of record in the main repo's `docs/` folder, then copy
the updated files here and push.
